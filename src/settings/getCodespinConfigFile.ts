// import * as path from "path";
// import { getConfigFilePath } from "./getConfigFilePath.js";

// export async function getCodespinConfigFile(
//   configDirFromArgs: string | undefined,
//   searchHomeDir: boolean
// ): Promise<string | undefined> {
//   const configDir = await getConfigFilePath(
//     configDirFromArgs,
//     searchHomeDir
//   );

//   if (configDir) {
//     return path.join(configDir, "codespin.json");
//   } else {
//     return undefined;
//   }
// }
